add_root_subdirectory(gui)
add_root_subdirectory(ged)
add_root_subdirectory(fitpanel)
add_root_subdirectory(guibuilder)
add_root_subdirectory(guihtml)
add_root_subdirectory(recorder)
add_root_subdirectory(sessionviewer)
if(root7)
  add_root_subdirectory(webdisplay)
  if(cefweb)
  add_root_subdirectory(cefdisplay)
  endif()
  if(qt5web)
  add_root_subdirectory(qt5webdisplay)
  endif()
  add_root_subdirectory(canvaspainter)
  add_root_subdirectory(fitpanelv7)
endif()
if(qtgsi)
  add_root_subdirectory(qtgsi)
endif()
if(qt)
  add_root_subdirectory(qtroot)
endif()
