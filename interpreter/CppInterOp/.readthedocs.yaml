version: 2

sphinx:
  configuration: docs/conf.py
  builder: html

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.11"
  apt_packages:
    - clang-20
    - cmake
    - libclang-20-dev
    - llvm-20-dev
    - llvm-20-tools
