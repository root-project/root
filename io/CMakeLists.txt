add_root_subdirectory(io) # special CMakeLists.txt
add_root_subdirectory(rootpcm)
add_root_subdirectory(sql)
add_root_subdirectory(xml)
if(xml)
  add_root_subdirectory(xmlparser)
endif()
if(castor)
  add_root_subdirectory(castor)
endif()
if(rfio)
  add_root_subdirectory(rfio)
endif()
if(gfal)
  add_root_subdirectory(gfal)
endif()
if(dcache)
  add_root_subdirectory(dcache)
endif()
if(hdfs)
  add_root_subdirectory(hdfs)
endif()
