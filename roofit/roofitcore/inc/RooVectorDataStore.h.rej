***************
*** 248,259 ****
        } else {
  	_vec.resize(siz);
        }
-       _vec0 = &_vec.front();
      }
  
      void reserve(Int_t siz) {
        _vec.reserve(siz);
-       _vec0 = &_vec.front();
      }
  
    protected:
--- 248,259 ----
        } else {
  	_vec.resize(siz);
        }
+       _vec0 = _vec.size() > 0 ? &_vec.front() : 0;
      }
  
      void reserve(Int_t siz) {
        _vec.reserve(siz);
+       _vec0 = _vec.size() > 0 ? &_vec.front() : 0;
      }
  
    protected:
***************
*** 538,549 ****
        } else {
  	_vec.resize(siz);
        }
-       _vec0 = &_vec.front();
      }
  
      void reserve(Int_t siz) {
        _vec.reserve(siz);
-       _vec0 = &_vec.front();
      }
  
      void setBufArg(RooAbsCategory* arg) { _cat = arg; }
--- 538,549 ----
        } else {
  	_vec.resize(siz);
        }
+       _vec0 = _vec.size() > 0 ? &_vec.front() : 0;
      }
  
      void reserve(Int_t siz) {
        _vec.reserve(siz);
+       _vec0 = _vec.size() > 0 ? &_vec.front() : 0;
      }
  
      void setBufArg(RooAbsCategory* arg) { _cat = arg; }
