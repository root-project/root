add_root_subdirectory(tmva)
add_root_subdirectory(tmvagui)
if(python AND NUMPY_FOUND)
   add_root_subdirectory(pymva)
elseif(python AND NOT NUMPY_FOUND)
   message(STATUS "TMVA: Deactivate PyMVA because numpy is not found")
endif()
if(r)
  add_root_subdirectory(rmva)
endif()
