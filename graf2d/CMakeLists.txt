if(root7)
  add_root_subdirectory(primitives)
  add_root_subdirectory(gpadv7)
endif()
add_root_subdirectory(gpad)
add_root_subdirectory(graf)
add_root_subdirectory(postscript)
add_root_subdirectory(mathtext)
if(WIN32)
  add_root_subdirectory(win32gdk)
elseif(x11)
  add_root_subdirectory(x11)         # special CMakeLists.txt
  add_root_subdirectory(x11ttf)      # special CMakeLists.txt
endif()
if(asimage)
  add_root_subdirectory(asimage)     # special CMakeLists.txt
endif()
if(qt)
  add_root_subdirectory(qt)
endif()
if(gviz)
  add_root_subdirectory(gviz)
endif()
if(fitsio)
  add_root_subdirectory(fitsio)
endif()
if(cocoa)
  add_root_subdirectory(quartz)
  add_root_subdirectory(cocoa)
endif()
