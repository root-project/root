if(mysql)
  add_root_subdirectory(mysql)
endif()
if(oracle)
  add_root_subdirectory(oracle)
endif()
if(odbc)
  add_root_subdirectory(odbc)
endif()
if(pgsql)
  add_root_subdirectory(pgsql)
endif()
if(sqlite)
  add_root_subdirectory(sqlite)
endif()
