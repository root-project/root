add_root_subdirectory(mathcore)
if(mathmore)
  add_root_subdirectory(mathmore)
endif()
add_root_subdirectory(matrix)
add_root_subdirectory(minuit)
if(minuit2)
  add_root_subdirectory(minuit2)
endif()
add_root_subdirectory(fumili)
add_root_subdirectory(physics)
add_root_subdirectory(mlp)
add_root_subdirectory(quadp)
add_root_subdirectory(foam)
add_root_subdirectory(smatrix)
add_root_subdirectory(splot)
#add_root_subdirectory(mathcore/test EXCLUDE_FROM_ALL)
add_root_subdirectory(genvector)
if(tmva)
  add_root_subdirectory(genetic)
endif()
if(unuran)
  add_root_subdirectory(unuran)
endif()
if(fftw3)
  add_root_subdirectory(fftw)
endif()

if(r)
  add_root_subdirectory(rtools)
endif()

add_root_subdirectory(vecops)
