if(python)
  if(NOT pyroot_experimental)
    add_root_subdirectory(pyroot)
  else()
    add_root_subdirectory(pyroot_experimental)
  endif()
endif()
if(ruby)
  add_root_subdirectory(ruby)
endif()
if(r)
  add_root_subdirectory(r)
endif()
